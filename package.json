{
  "name": "burgers-only",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://github.com/xaviermaillou/burgers-only#readme",
  "bugs": {
    "url": "https://github.com/xaviermaillou/burgers-only/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xaviermaillou/burgers-only.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "clean": "node scripts/clean-generated.mjs",
    "build": "npm run clean && npm run build:prepare && npm run build:app && npm run build:ssg",
    "build:prepare": "node scripts/prepare-dist.mjs",
    "build:app": "esbuild app.js --bundle --format=esm --minify --sourcemap --outdir=dist/assets",
    "build:ssg": "node scripts/generate-static-pages.mjs",
    "test": "test"
  },
  "dependencies": {
    "@growthbook/growthbook": "^1.6.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
